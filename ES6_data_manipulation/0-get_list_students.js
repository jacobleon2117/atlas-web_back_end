export default function getListStudents() {
  const s1 = { firstName: 'Guillaume', id: 1, location: 'San Francisco' };
  const s2 = { firstName: 'James', id: 2, location: 'Columbia' };
  const s3 = { firstName: 'Serena', id: 5, location: 'San Francisco' };

  const studentArray = [s1, s2, s3];

  return studentArray;
}
